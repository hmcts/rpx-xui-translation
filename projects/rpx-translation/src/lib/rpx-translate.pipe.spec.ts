import { RpxTranslatePipe } from './rpx-translate.pipe';

describe('RpxTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new RpxTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
